export default interface Armor {
  head?: {
    name: string
    icon: string
    link: string
  }

  chest?: {
    name: string
    icon: string
    link: string
  }

  arms?: {
    name: string
    icon: string
    link: string
  }

  legs?: {
    name: string
    icon: string
    link: string
  }
}